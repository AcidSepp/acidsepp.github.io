## Install Git

- Install [Git](https://git-scm.com/install/mac):

```zsh
brew install git
```

- Verify the installation:

```zsh
git -v
```
- The output should look like this:

```zsh
git version 2.51.0
```
